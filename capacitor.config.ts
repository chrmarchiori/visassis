import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.visassis',
  appName: 'visassis',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
